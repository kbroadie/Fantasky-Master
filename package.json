{
  "name": "fantasky-master",
  "private": true,
  "type": "module",
  "description": "Tooling only: the app itself has no build step or dependencies.",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "check": "node tools/check-data.mjs",
    "shots": "node tools/screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
